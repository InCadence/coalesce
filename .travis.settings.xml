<Settings Xmlns="Http://Maven.Apache.Org/SETTINGS/1.0.0"
          Xmlns:Xsi="Http://Www.W3.Org/2001/XMLSchema-Instance"
          Xsi:SchemaLocation="Http://Maven.Apache.Org/SETTINGS/1.0.0
                      Http://Maven.Apache.Org/Xsd/Settings-1.0.0.Xsd">

<servers>


  <!-- These crendetials are required to compile the Elastic persister which has a dependency on IronHide -->
  <server>
    <id>devforce</id>
    <username>${env.devforce_user}</username>
    <password>${env.devforce_password}</password>
  </server>

  
  <!-- These credentials are only required when running mvn deploy -->
  <server>
    <id>nexus-public</id>
    <username>${env.nexus_user}</username>
    <password>${env.nexus_pass}</password>
  </server>


</servers>

</Settings>
